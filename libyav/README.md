# libyav

Minimal C++ library target for YetAnotherVoronoi.

## Dependencies

- Boost headers (`boost::geometry`)

## Build from root

```bash
cmake -S . -B build
cmake --build build --target libyav
```
